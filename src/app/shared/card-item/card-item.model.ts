import { CardItemType } from "app/shared/card-item/card-item.enum";

export class CardItem {
    title: string;
    subtitle: string;
    description: string;
    icon: string;
    type: CardItemType
}